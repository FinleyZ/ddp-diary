# Recent activity (pre-extracted, bounded)

The following is a bounded summary of Claude Code session activity on this machine for
the target date, extracted by the orchestrator — not by you — from
`~/.claude/projects/*.jsonl`. It is already sliced to this date and capped in size.
Treat it as your primary "what did I do today" input; do not attempt to read raw
session transcripts yourself, and do not assume anything happened outside what's shown
here.

If this section is empty below, no session activity was found for the target date on
this machine — say so honestly in the entry rather than inventing activity.
